# AeroTwin AI - Operator HMI environment template
# Copy to .env and adjust. Never commit .env.

# "LIVE" connects to the real Control API via REST + Socket.IO.
# "DEMO" (default) uses embedded mock data — no backend needed.
VITE_HMI_MODE=DEMO

VITE_CONTROL_API_URL=http://localhost:4000
